public interface Animal {

    //public static final - true for all variables in interface - bad practice
    int legs=0;

    //method not defined - all method in interface are abstract
    void makeSound();
}
